[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify-functions"

[build.environment]
  NODE_VERSION = "22"

[dev]
  command = "npm run dev"
  port = 5173
  targetPort = 5173
  framework = "vite"
